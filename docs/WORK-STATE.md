# Evergreen: checkpoint

Updated 11.09.2026. Canonical source: `kurotermoon-oss/evergreen_market`, branch `main`. Use the current device's clone, not the historical Desktop path.

- Current task: GitHub/device synchronization, public storefront bug audit and responsive fixes; owner explicitly requested deployment.
- Device setup: the current ZIP copy exactly matched GitHub `53e5cc6`; restored Git history, origin/main tracking and local pull.ff=only without replacing working files. Git fetch works with OpenSSL/HTTP 1.1. GitHub connector can publish. Automatic approval review rejected changing .git ownership; no ownership/ACL changes were made. Sync commands use only a path-scoped Git invocation.
- Added `npm run sync` (clean-main fast-forward only) and `npm run sync:check`; dirty/ahead copies stop without reset. Cross-device instructions: `DEVICE-WORKFLOW.md`; AGENTS and active code map use GitHub as canonical source.
- Fixed: concurrent checkout requests; optional local history/admin refresh masking successful orders; blocked/full/malformed browser storage; login/registration failing after successful authentication when order history fails; pagination pointing past the last page after stock refresh. Confirmation now reports local history persistence accurately.
- Responsive fixes: card actions adapt to available width with stable height after adding; 320px fulfillment label no longer splits words around its count; mobile product purchase buttons use complete shorter labels. Checkout contact fields have accessible names and phone keyboard; inactive delivery is no longer advertised in account benefits.
- Checks: 23 tests pass (`npm run test:storefront`), including seven regressions that failed before fixes; production build; diff whitespace check. Browser: catalog/card actions 320/390/768/1024/1280, product and checkout 320/390/768/1280, home/guide/contacts/login 320/1280. No page overflow observed. Local mock checkout produced exactly one request, disabled the button while pending and displayed confirmation; no real order/account/admin writes or supplier sync.
- Deployment verified: code commit `018ca222052a9fa9ad31d2b39bf497501f3cbc2a` is live on evergreenmarket.com.ua. Main JS, CSS, CartView, ProductDetailsView and SuccessView assets return HTTP 200 and match local SHA-256 byte-for-byte; /api/health returns 200/ok. Public catalog layout checked at 320/1280. Local main matched GitHub, clean sync command passed.
- Data observations: the 300g Чізо Сулугуні product has 1kg text in its long description; needs source-backed catalog correction, not an invented replacement. Previous checkpoint recorded three MilkDiller 404 mappings (see SUPPLIER_AVAILABILITY_SYNC.md); no remapping attempted here. Existing six-hour Railway Cron and stock/business rules preserved.
- Next task: sync from GitHub, read this checkpoint only when resuming, then follow the owner's new scope. Prior redesign/mascot work is complete.
