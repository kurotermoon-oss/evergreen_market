# Evergreen: checkpoint

Updated 17.09.2026. Base: `kurotermoon-oss/evergreen_market`, `main`, `2c1cae3`. Synced this device by fast-forward; existing untracked `docs/CATALOG-QUALITY-PRICING.md` is unrelated and must not be uploaded.

- Implemented administrative Telegram Mini App at `/telegram/admin`: active/history lists, search/status filters, pagination, order details with historical line prices and current supplier/stock information, confirmed status actions, cancellation reason, minute/return refresh, Telegram back button and safe areas. Lazy entry; isolated orders-only API/session for a separate admin bot. No customer Mini App or catalogue editing added.
- Server verifies signed/fresh initData and explicit Telegram ID allowlist; 30-minute in-memory client session with permission checks on every request. Admin Telegram sessions cannot access the full admin API. Staff IDs stay in admin history and are stripped from customer history; customer order endpoint uses the existing price sanitizer.
- Website and Mini App share order actions. PostgreSQL row lock and transition checks prevent duplicate stock restoration/finalization and stale actions. Notification failure does not undo saved state. New-order admin notifications are best effort and link to the exact order; existing customer bot remains separate.
- Checks: production build, 34 tests, diff checks; browser at 320/390/768/1280, search, confirmation, ready/complete, cancel validation/Escape/focus, hidden/deleted items, unsafe URLs, conflict and expired-session simulations. Reduced-motion CSS reviewed, OS preference not emulated. PostgreSQL concurrency tested with an isolated repository double, not a live DB.
- Next: follow `docs/TELEGRAM_ADMIN.md` to create/configure the separate bot, set token/allowed IDs/HTTPS app URL and menu button, then test real Telegram iOS/Android. Credentials/IDs were not supplied. No real orders/messages, supplier runs or migrations were performed. Customer Telegram end-to-end activation remains unverified.
