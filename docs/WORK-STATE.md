# Evergreen: checkpoint

Updated 12.09.2026. Canonical source: `kurotermoon-oss/evergreen_market`, branch `main`. Use the current device's clone and `DEVICE-WORKFLOW.md`.

- Current task: restore catalog subcategories while preserving the owner's preferred category dialog and customer profile redesign. Started from remote main `12327aa`; no frontend/profile redesign in this fix.
- Root cause: PostgreSQL `buildProductData` treated an omitted subcategory in PATCH as null. The earlier batch of content/price corrections therefore cleared 313 relations. Prior verification missed the subcategory field; the API categories themselves remained intact, but count-based navigation hid children with no linked goods.
- Fix: omitted subcategory preserves the existing relation when the parent is unchanged; explicit empty/null/all/none clears it; moving to another category without a child resets it. Explicit selection/new-child creation remains supported. The same omission-versus-null issue for clearing oldPrice is fixed.
- Live repair completed: compared before/after snapshots from the prior batch, matched all 313 lost relations against current products and admin categories, restored only empty relations with the same parent and valid original child. Verified all restored IDs and unchanged other content, prices, fulfillment, supplier and merchandising fields. Current data was not replaced with the old snapshot.
- Private recovery plan, before/after copies and applied-ID journal remain under the old task workspace's ignored `private-audit/subcategory-repair` folder; never upload these or credentials.
- Checks: 7 new isolated repository regressions, 10 existing storefront tests and 13 supplier-sync tests passed. Live desktop category → lactose-free selection returns 6 matching products; mobile dialog at 390/320 shows all 5 milk subcategories with no horizontal overflow at 320. No real orders/accounts created.
- Publication: reviewed server/test/checkpoint changes ready for GitHub; verify remote main and Railway deployment separately. Live relation repair is already complete.
- Preserve earlier work: profile commit `12327aa`, category/help commit `7148b62`, checkout/session/storage fixes, guest checkout, supplier minima, minute catalog refresh and Railway six-hour cron. Owner deferred registration changes, minimum changes and purchase schedules.
- Earlier pricing/content report remains local and is not part of this publication. Its previous claim of protected-field verification did not cover subcategory; the defect and correction are documented above. Remaining source questions include syrup labels, caramel Alpro composition and three old MilkDiller URLs; do not infer replacements.
